
namespace: run-tracker

resources:
  - ../../base
  - ../../deploy-affinity
  - ../../service

patches:
  - patch: |-
      - op: replace
        path: /spec/type
        value: LoadBalancer   
    target:
      kind: Service
      name: svc-1